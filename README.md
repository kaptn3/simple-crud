# Simple CRUD system

## Usage

### Installation

Install the npm dependencies:

```npm i```

Start server:

```npm run dev```

Access: <localhost:3000>

You can change port: edit variable port in `app.js`.